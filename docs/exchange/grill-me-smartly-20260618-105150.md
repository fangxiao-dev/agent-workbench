# grill-me-smartly review

## Source

- Task: Review the TaskManager Git + multi-project dashboard plan before implementation.
- Source file/context: `docs/plans/2026-06-18-taskmanager-git-multiproject-dashboard.md`
- Created at: 2026-06-18T10:51:50+02:00

## Plan Snapshot

The plan turns `D:\CodeSpace\TaskManager` into a standalone Git-tracked multi-project Obsidian dashboard vault. It uses stable configured project IDs, per-project task/source directories, reusable templates, project/global Bases, and script-driven initialization/import/validation.

## Proposed Questions

| ID | Question | Main-session recommended answer |
| --- | --- | --- |
| Q1 | Is the Git track/ignore list enough to restore the Obsidian dashboard after clone without tracking machine-local state? | Amend the plan to track dashboard-required Obsidian config and keep local project roots out of tracked files. |
| Q2 | Is `项目ID + 来源相对路径` enough to represent both project repo docs and TaskManager vault source notes? | Amend the plan to add a source namespace via `来源类型`, with per-type relative-path semantics. |

## Subagent Answer

| ID | Answer | Evidence | Uncertainty |
| --- | --- | --- | --- |
| Q1 | Not fully sufficient as written. Track basics are mostly covered, but local roots in tracked `projects.yml` are machine-local. Add explicit tracking for GoodBases styles and new Obsidian property types. | `.obsidian/community-plugins.json` enables `good-bases` and `tray`; `core-plugins.json` enables Bases/templates; `tray/data.json` is local startup/tray preference; GoodBases includes `main.js`, `manifest.json`, `styles.css`; current `types.json` lacks new project/source fields. | Obsidian was not launched; conclusion is based on file inspection. |
| Q2 | Partially sufficient, but ambiguous for vault source notes. Existing tasks point both to repo impl plans and to `D:\CodeSpace\TaskManager\20_Sources` notes. | Current tasks include repo-doc `来源` under `D:/CodeSpace/prj-supplyer-webapp/docs/impl-plans/...` and vault-note `来源` under `D:/CodeSpace/TaskManager/20_Sources/...`; source notes are first-class sources and may contain repo/worktree related paths. | Some body links may still contain absolute paths after frontmatter migration. |

## Decision Packet

- Resolved locally: Q1 and Q2.
- Needs real user: none for the current plan revision.
- Recommended decision: keep `projects.yml` tracked but without local roots; write local roots to ignored `00_Config/projects.local.yml`; track app/appearance/snippets/types/plugin files needed for dashboard restore; model source resolution as `项目ID + 来源类型 + 来源相对路径`.
- Plan amendments applied to `docs/plans/2026-06-18-taskmanager-git-multiproject-dashboard.md`.
