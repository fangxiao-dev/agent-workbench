interface:
  display_name: "Owner Thread Broker"
  short_description: "Broker Owner decisions and maintain thread session routing"
  default_prompt: "Read ../SUB-SKILL.md to coordinate an Owner decision with controller and child threads."
