# Session Exchange Readiness

The service performs a session exchange before writing the Gate Ledger. The environment identity must bind to the actual test resources. All four reference integrations must be verified in the same snapshot before the package can close.
