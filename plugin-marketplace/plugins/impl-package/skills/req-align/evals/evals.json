{"skill_name":"req-align","evals":[
  {"id":1,"prompt":"为一个目标模块和外部 provider 选择都未定的新需求建立 implementation package。","expected_output":"Records the unresolved Decision and stops before claiming Spec readiness.","files":[],"expectations":["Treats the owner choice as blocking","Does not initialize runtime state","Uses readable D/S aliases only after artifacts exist"]},
  {"id":2,"prompt":"仅做 Spec 设计。现有 Decision Gate 已 PASSED 且仍覆盖当前需求：新增一个需要权限、幂等键、optimistic concurrency、持久化聚合和公开读取模型的 HTTP capability。","expected_output":"Enters spec-only, rebuilds the current design scope and freezes behavior plus implementation-ready API/persistence/read-model contracts before evaluating the shared Spec Gate.","files":[],"expectations":["Accepts valid current Decision evidence without rerunning full alignment","Lists API operations, persistence models, seams and public read models before formal design","Freezes DTO required/nullability, identity, uniqueness, CAS and stable errors","Uses contract-design.md only if its earned condition is met","Keeps proposal, approval, actual S revision and Gate/handoff status consistent","Does not create plan or execution state"]},
  {"id":3,"prompt":"仅做 Spec 更新，但现有 Decision 选择的 provider 已被本次需求替换，且 owner 尚未决定新 provider。","expected_output":"Blocks spec-only because the current Decision evidence is invalidated by the delta.","files":[],"expectations":["Does not silently switch to the full route","Names the missing owner Decision","Does not create or update Spec, Plan or runtime state"]},
  {"id":4,"prompt":"仅做 Spec 设计。Decision 已 PASSED；变化只是一个模块内部的纯领域计算规则，没有 API、持久化、跨模块 seam 或公共 read model。","expected_output":"Keeps a single spec.md, records a current empty contract-surface scope and evaluates behavior/acceptance completeness.","files":[],"expectations":["Does not create contract-design.md","Still rebuilds Spec 设计范围 first","Does not manufacture API, DTO or persistence contracts","Keeps proposal, approval, actual S revision and Gate/handoff status consistent"]},
  {"id":5,"prompt":"仅做 Spec 设计。一个异步作业的 start operation 返回 jobId，但 complete operation 的 required request 还需要 opaque completionToken；当前没有 response、read model 或调用方输入能提供该 token。","expected_output":"Finds the interaction-closure break and blocks or repairs the Spec before planning without prescribing an unrelated implementation architecture.","files":[],"expectations":["Traces every required input to an obtainable source","Names completionToken as the minimal contract break","Does not pass the Spec while the input is unreachable","Does not create Plan, Task or a new mandatory artifact"]},
  {"id":6,"prompt":"仅做 Spec 设计。草案写着 create、replace、validate、commit 全部 mutation 都使用 durable Idempotency-Key，但没有逐 operation 说明 natural identity、CAS、exact retry、相同 key 不同 payload、响应丢失后的恢复或 stale 结果。","expected_output":"Requires per-operation observable retry and concurrency semantics while allowing each operation to use the smallest mechanism justified by its risk.","files":[],"expectations":["Rejects the blanket mechanism as insufficient contract semantics","Does not require durable replay for every mutation by default","Closes identity, duplicate/stale result and recovery only where applicable","Does not modify planning or task workflow"]},
  {"id":7,"prompt":"仅做 Spec 设计。public detail 承诺 auditDigest 和 currentRevision，但现有 persistence/read seams 没有 producer；commit request 又同时提交 currentRevision，而 server attempt 已冻结同一事实，Spec 没有说明谁是 authority。","expected_output":"Finds both the unrealizable public output and duplicate-authority ambiguity, then blocks or repairs the Spec before planning.","files":[],"expectations":["Requires an authoritative producer for promised public fields","Requires one authority or explicit precedence for duplicated facts","Does not treat a mapper or controller as an authority","Keeps the rule general rather than specializing it to one product journey"]}
]}
